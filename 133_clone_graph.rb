        #  visit the start, make a copy and connect it to new Nodes
        #   q up the neighbors
        # visited = Set.new()
        # q = [node]
        # while q.length > 0
        #     curr = q.shift()
        #     visited.add(curr)
        #     curr.neighbors.each {|neighbor|
        #         if !visited.include?(neighbor)
        #             q << neighbor
        #         end
        #     }
        # end

        # need to make new neighbors an entry but it shouldnt be in visited

